<nav class='navbar navbar-default navbar-fixed-top'>
  <div class="navbar-container">
    <div class="navbar-header">
      <button type="button" name="button" class='navbar-toggle collapsed' data-target= '#bs-navbar-collapse-1' data-toggle="collapse">
        <span class='sr-only'>Toggle Navigation</span>
        <span class='icon-bar'></span>
        <span class='icon-bar'></span>
      </button>
    </div>
    <div id="bs-navbar-collapse-1" class="collapse navbar-collapse">
      <ul class='nav navbar-nav navbar-right'>
        <% if user_signed_in? %>
          <li><%= link_to "Browse", items_path%></li>
          <li><%=link_to "My Orders", orders_path%></li>
          <li> <%=link_to "New Item", new_item_path%> </li>
          <li><%= link_to "Logout", destroy_user_session_path, method: :delete%></li>
        <%else%>
          <li> <%=link_to "Login", new_user_session_url%> </li>
          <li> <%=link_to "Register", new_user_registration_path %> </li>
        <%end%>
      </ul>
    </div>
  </div>
</nav>
